# Static assets

Put your **favicon** here:

- **`loop-1-svgrepo-com.svg`** — favicon (SVG).

Templates link to it via `url_for('static', filename='loop-1-svgrepo-com.svg')`.  
Other static files (images, CSS, JS) can also go in this folder and be served at `/static/filename`.
